# Database connection settings for tests
PGHOST=localhost
PGPORT=5433
PGDATABASE=schema_compare_test
PGUSER=testuser
PGPASSWORD=testpass
PGCONNECT_TIMEOUT=10

# Test control flags
RUN_DB_TESTS=1
DB_TEST_VERBOSE=0
